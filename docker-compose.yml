services:
  moonbags:
    build: .
    restart: unless-stopped
    env_file: .env
    ports:
      - "8787:8787"
    volumes:
      # Persist positions and settings across container restarts
      - ./state:/app/state
